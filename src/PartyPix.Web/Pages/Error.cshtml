@page "{code:int?}"
@model ErrorModel
@{
    ViewData["Title"] = "Something went wrong";
    Layout = "_Layout";
}
<section class="mx-auto max-w-2xl px-4 py-20 text-center">
    <h1 class="text-3xl font-semibold">@(Model.Code?.ToString() ?? "Error")</h1>
    <p class="mt-2 text-slate-600">@Model.Message</p>
    <a href="/" class="mt-6 inline-block text-indigo-600 hover:underline">Go home</a>
</section>
